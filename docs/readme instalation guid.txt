istall windows server 2019 
set the static ip address eg (192.168.1.200)
install IIS on the server
install node js 18.0 version
download browser edge / chrome
install visual studio 2019 x64 redistibution 
install mysql community version 8.2.0
	after mysql installation completed set root password to giit (or any)  
	check mysql server start when windows start
	install iisnode 
	install IIS 
	install url_rewrite 
copy McuExamApi folder into c:\inetpub folder
open command prompt and change the directory to c:\inetpub\McuExamApi
run command npm i  (install dependent node js packages)
open iis manger 
	
	
	